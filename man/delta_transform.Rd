% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experimental_api.R
\name{delta_transform}
\alias{delta_transform}
\title{Apply delta (temporal difference) transform}
\usage{
delta_transform(
  data,
  order = 1L,
  method = c("forward", "backward", "central"),
  remove_mean = TRUE
)
}
\arguments{
\item{data}{Matrix with time in rows, variables in columns}

\item{order}{Difference order: 1 (first differences), 2 (second differences), etc.}

\item{method}{Differencing method: "forward", "backward", "central"}

\item{remove_mean}{Remove mean before differencing (default: TRUE)}
}
\value{
Matrix of temporal differences (n_timepoints - order rows)
}
\description{
Compute temporal differences to remove slow trends and emphasize
rapid changes. This is commonly used for preprocessing fMRI data.
}
\examples{
\donttest{
# Create example time series data (50 timepoints, 100 variables)
set.seed(123)
my_timeseries <- matrix(rnorm(50 * 100), nrow = 50, ncol = 100)
# Add some trend
my_timeseries <- my_timeseries + outer(1:50, rep(1, 100))

# First differences for trend removal
diff_data <- delta_transform(my_timeseries, order = 1)

# Second differences for acceleration analysis
accel_data <- delta_transform(my_timeseries, order = 2, method = "central")
}

}
