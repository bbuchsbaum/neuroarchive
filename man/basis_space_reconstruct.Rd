% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experimental_api.R
\name{basis_space_reconstruct}
\alias{basis_space_reconstruct}
\title{Reconstruct data from spatial basis embedding}
\usage{
basis_space_reconstruct(
  embedded_result,
  mask,
  subset_time = NULL,
  subset_voxels = NULL
)
}
\arguments{
\item{embedded_result}{Result from \code{basis_space_embed}}

\item{mask}{Original mask (must match embedding)}

\item{subset_time}{Optional time indices to reconstruct}

\item{subset_voxels}{Optional voxel indices to reconstruct}
}
\description{
Reconstruct data from spatial basis embedding
}
\examples{
\donttest{
# Create example data and mask
library(neuroim2)
dims <- c(20, 20, 10)
arr <- array(TRUE, dims)
mask <- LogicalNeuroVol(arr, NeuroSpace(dims, spacing = c(2, 2, 2)))

# Create time series data
set.seed(123)
n_voxels <- sum(as.logical(mask))
my_data <- matrix(rnorm(50 * n_voxels), nrow = 50, ncol = n_voxels)

# Embed the data
embedded <- basis_space_embed(my_data, mask)

# Full reconstruction
reconstructed <- basis_space_reconstruct(embedded, mask)

# Partial reconstruction (first 20 timepoints)
partial_time <- basis_space_reconstruct(embedded, mask, subset_time = 1:20)

# Spatial subset reconstruction
partial_voxels <- basis_space_reconstruct(embedded, mask, subset_voxels = 1:100)
}

}
