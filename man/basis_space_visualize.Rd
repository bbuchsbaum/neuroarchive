% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experimental_api.R
\name{basis_space_visualize}
\alias{basis_space_visualize}
\title{Visualize spatial basis functions}
\usage{
basis_space_visualize(
  basis_result,
  mask,
  n_show = 9,
  slice_axis = c("axial", "coronal", "sagittal"),
  slice_position = NULL,
  color_palette = "viridis"
)
}
\arguments{
\item{basis_result}{Result from \code{basis_space_centers} or basis matrix}

\item{mask}{LogicalNeuroVol for spatial context}

\item{n_show}{Number of basis functions to visualize (default: 9)}

\item{slice_axis}{Axis for slicing: "axial", "coronal", "sagittal"}

\item{slice_position}{Position along slice axis (default: middle)}

\item{color_palette}{Color palette for visualization}
}
\value{
Grid of basis function visualizations
}
\description{
Create visualizations of spatial HRBF basis functions for inspection
and quality control. Useful for understanding spatial coverage
and basis characteristics.
}
\examples{
\donttest{
# Create example mask and basis
library(neuroim2)
dims <- c(20, 20, 10)
arr <- array(TRUE, dims)
mask <- LogicalNeuroVol(arr, NeuroSpace(dims, spacing = c(2, 2, 2)))

# Generate basis for visualization
my_basis <- basis_space_centers(mask, sigma0 = 4, levels = 2)

# Visualize first 9 basis functions
basis_viz <- basis_space_visualize(my_basis, mask)

# Focus on specific slice and functions
slice_viz <- basis_space_visualize(
  my_basis, 
  mask,
  n_show = 4,
  slice_axis = "axial",
  slice_position = 5
)
}

}
